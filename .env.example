# Database (cPanel MySQL/MariaDB)
# Replace with your actual password and host
DATABASE_URL="mysql://corpora5_webprometrics:__REPLACE_PASSWORD__@localhost:3306/corpora5_webprometricske?connection_limit=5&pool_timeout=15"

# Example (your provided credentials)
# DATABASE_URL="mysql://corpora5_webprometrics:5eJF)*QROqRTisR@localhost:3306/corpora5_webprometricske?connection_limit=5&pool_timeout=15"

# JWT / Secrets
JWT_SECRET="change-me-in-production"
TOKEN_ENCRYPTION_KEY="change-me-too"

# App
NODE_ENV=production
PORT=8080
ALLOWED_ORIGINS=

# Initial Admin Seed (optional)
# Used only when no users exist yet
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=admin123

# OAuth placeholders (fill in when ready)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REDIRECT_URI=

# Meta (Facebook) Ads OAuth
META_APP_ID=
META_APP_SECRET=
META_REDIRECT_URI=

# X (Twitter) OAuth2
TWITTER_CLIENT_ID=
TWITTER_REDIRECT_URI=

# LinkedIn OAuth
LINKEDIN_CLIENT_ID=
LINKEDIN_CLIENT_SECRET=
LINKEDIN_REDIRECT_URI=

# Google Ads API
GOOGLE_ADS_DEVELOPER_TOKEN=

# Webhooks
WEBHOOK_SECRET=

# Frontend (dev only)
# VITE_API_URL=http://localhost:8080/api
